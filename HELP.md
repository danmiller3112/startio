# StartIO-app
```
Hi there.
1. mvn clean install
2. Just run this application  [ServerApplication.main()]


Test API endpoint using cURL or a tool like Postman:
    curl --location 'http://localhost:8080/api/listings?min_price=0.0081&max_price=0.15&min_min_cpm=1&max_min_cpm=2'